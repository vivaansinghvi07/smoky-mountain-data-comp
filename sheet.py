import openpyxl

def make_sheet():
    # gets rows from varrun - assume the list of rows are in this format
    # [['item', 'item'], ['item', 'item']]

    # todo: 
    # create a sheet and write each row of the list in the sheet
    # save the sheet